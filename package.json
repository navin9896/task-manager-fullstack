{
  "name": "ethara-ai-assignment",
  "version": "1.0.0",
  "description": "Production-ready full-stack task manager with clean architecture:",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend/task-manager",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm start --prefix frontend/task-manager",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "echo build handled by vercel"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
